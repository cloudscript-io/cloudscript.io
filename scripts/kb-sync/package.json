{
  "name": "cloudscript-kb-sync",
  "version": "1.0.0",
  "private": true,
  "description": "Mirrors the cloudscript.io user guides into the Cloudscript Help knowledge base space in Confluence. Installed only by the kb-sync GitHub Actions workflow; the website build never touches it.",
  "type": "module",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test ../../tests/kb-sync/*.test.mjs",
    "dry-run": "node sync.mjs --dry-run"
  },
  "dependencies": {
    "parse5": "8.0.1"
  }
}
